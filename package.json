{
  "name": "tradestation-tax-calculator",
  "version": "0.0.2",
  "description": "Libreria per calcolare le tasse da pagare per i conti Tradestation",
  "private": false,
  "main": "./dist/bin.js",
  "bin": {
    "ttc": "./dist/bin.js"
  },
  "scripts": {
    "build": "esbuild --bundle src/bin.ts --platform=node --outfile=dist/bin.js --packages=external",
    "ci": "pnpm run build",
    "release": "pnpm run ci && changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chokidar": "^3.5.3",
    "commander": "^10.0.1",
    "fast-glob": "^3.2.12"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "@types/node": "^20.1.2",
    "esbuild": "^0.17.18",
    "typescript": "^5.0.4"
  }
}
